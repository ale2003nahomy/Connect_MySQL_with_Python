iniciar git y github

*iniciar git
git init

*configurar mi usuario
git config --global user.name "German"
git config --global user.email "germanedusp@gmail.com"

*iniciar commit
git status    (estado de los archivos)

git add .
git commit -m "nombre del commit"

git log --oneline   //ver el historial de commit

git diff  //muestra lo que hemos agregado

git checkout (pego codigo del commit, para regresar a un anterior)
git checkout master  (regresa al ultimo commit)

* agregar un remoto
git remote add (alias) url de github

**subir los cambios
git push alias master

(pedira contraseña de github)


**bajar los cambios
git pull alias master

** ignorar archivos o carpeta
.gitignore  (crear ese archivo)
agregar el nombre de la carpeta o archivo


